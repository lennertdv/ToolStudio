{
  "entities": {
    "pageview": {
      "title": "Pageview",
      "description": "A record of a user visiting a specific tool page",
      "type": "object",
      "properties": {
        "toolId": { "type": "string" },
        "categoryId": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "path": { "type": "string" },
        "userAgent": { "type": "string" }
      },
      "required": ["toolId", "categoryId", "timestamp", "path"]
    },
    "admin": {
      "title": "Admin",
      "description": "An administrator account",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "role": { "type": "string" }
      },
      "required": ["email"]
    }
  },
  "firestore": {
    "pageviews": {
      "schema": "pageview",
      "description": "Collection of all pageviews for analytics"
    },
    "admins": {
      "schema": "admin",
      "description": "Registered administrators with dashboard access"
    }
  }
}
